package main
import (
		"fmt"
)

/* gen (p) genera un grafo casuale, a partire dalla probabilità p compresa tra 0 e 1 (inclusi).
Il modello matematico di riferimento è il seguente: si considerano tutti i possibili archi
includendoli nel grafo con probabilità p. Più esplicitamente, per ogni possibile coppia di
vertici, si genera un numero reale compreso tra 0 e 1; se questo è minore di p si inserisce
l’arco, altrimenti non lo si inserisce.
NB: potete usare questa operazione per generare grafi su cui testare la correttezza dei
vostri programmi!
*/

func main(){

}